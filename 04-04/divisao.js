const numero1 = 15
const numero2 = 0
function divisaoNaoZero(numero1,numero2)
    if (numero2 > 0) {
        return numero1 / numero2;
    } else {
        return "Divisão por zero não é permitida"
    }
    console.log (divisaoNaoZero)
